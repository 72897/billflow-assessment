# Normalise line endings so the repo is identical on Windows, macOS and Linux
# (and so Vercel's Linux builders see the same bytes that were committed).
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.pdf binary
